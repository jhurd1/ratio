#include "ratio.h"
/*************************************
 * Driver function for Ratio.
 * ***********************************/
int main(int argc, char *argv[])
{
    Ratio ra;
    const int p = ra.getI();
    ra.showRatio(p, ra.gettester());
    return 0;
}
