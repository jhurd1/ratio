#include "ratio.h"
/*************************************
 * Driver function for Ratio.
 * ***********************************/
int main(int argc, char *argv[])
{
    Ratio r;
    int i;
    std::vector<int> *temp = nullptr;
    r.showRatio(i, temp);
    return 0;
}
